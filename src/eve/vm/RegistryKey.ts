export enum RegistryKey {
  // simple registers -- locals
  A = 0x00,
  B = 0x01,
  C = 0x02,
  D = 0x03,
  E = 0x04,
  F = 0x05,
  G = 0x06,

  // some other, more different registers
  // x registers -- arguments
  // AX = 0x10,
  // BX = 0x11,
  // CX = 0x12,
  // DX = 0x12,

  // // z registers -- internal
  // AZ = 0x20,
  // BZ = 0x21,
  // CZ = 0x22,
  // DZ = 0x22,
}